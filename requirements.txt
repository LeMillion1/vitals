# ── Core (vitals/) ───────────────────────────────────────────────────────────
SQLAlchemy==2.0.51
asyncpg==0.31.0
alembic==1.18.5
python-dotenv==1.2.2
APScheduler==3.11.3
redis>=5.0,<6.0
greenlet==3.5.4
tzdata==2026.3
numpy>=1.26,<3.0
# Conflict-engine catalog (vitals/data/*.yaml) — was previously only a
# transitive dependency (uvicorn[standard]); now imported directly.
PyYAML>=6.0,<7.0

# ── Web delivery (FastAPI + Jinja PWA) ───────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
Jinja2>=3.1,<4.0
python-multipart>=0.0.9
bcrypt>=4.1,<6.0
itsdangerous>=2.1,<3.0
# Fernet, for the per-subject provider credentials in ``integration_credentials``
# — the one thing here that has to be encrypted rather than hashed, because a
# Garmin password has to come back out to be used. Already present transitively;
# pinned explicitly now that application code imports it.
cryptography>=47.0,<51.0
# 2FA enrolment QR. Pure Python, no dependencies of its own, renders straight to
# inline SVG — nothing to vendor into /static and no image pipeline.
segno>=1.6,<2.0
httpx>=0.27,<1.0
# Web Push payload encryption and VAPID signing. Exact because this protocol
# boundary is used by unattended image rebuilds; a new release must be reviewed
# against our no-PHI payload and provider-response contracts before adoption.
pywebpush==2.4.0

# ── LLM gateway (OpenRouter via the OpenAI-compatible SDK) ───────────────────
openai>=1.40,<2.0

# ── Integrations ─────────────────────────────────────────────────────────────
# Hevy uses httpx (above). Garmin has no official API — garminconnect drives a
# web session with its own token store; it's lazy-imported so the test suite
# doesn't need it.
# PINNED, and it must stay pinned: the Dockerfile resolves requirements on every
# rebuild, so an open range means an unattended `deploy.sh` can swap the login
# machinery underneath a working token. 0.3.x already dropped garth and changed
# the token format once — the client code below is written against 0.3.7.
garminconnect==0.3.7

# Labs: render a PDF page to an image for the vision LLM (no system deps,
# lazy-imported in labs_service).
PyMuPDF>=1.24,<2.0

# ── Model Context Protocol (MCP) ─────────────────────────────────────────────
# The official SDK, pinned exactly. Its 2.0.0 release *is* protocol revision
# 2026-07-28 — same day, and ``mcp.types.LATEST_PROTOCOL_VERSION`` says so — and
# the wire contract is the thing a connector on the other side depends on, so it
# is not a range. FastMCP 3.4.5 stood here and could not stay: it requires
# ``mcp<2.0``, so this is a replacement rather than an upgrade.
#
# It brings ``httpx2`` alongside our ``httpx``. Separate distributions with
# separate import names, so both live here without either being pinned by the
# other's needs — the Hevy client keeps the httpx it was written against.
mcp==2.0.0
